arr = [45, 54, 76, 123, 25, 123] 
max_value = max(arr) # finds the maximum value in one line
print(f"The Maximum element in the array is: {max_value}")

# Reversing a list is also very simple
reversed_arr = arr[::-1]
print(f"The reversed array is: {reversed_arr}")
